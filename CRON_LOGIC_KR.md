# StockWeb Cron Job Logic Documentation (KR)

이 문서는 현재 시스템에서 동작하는 자동화 작업(Cron Jobs)의 스케줄과 로직을 설명합니다. 모든 시간은 **미국 동부 표준시 (EST/EDT)** 기준입니다.

## 1. 정규장 (Market Hours)

- **파일**: `cron/intraday.py` -> `run_market_hours_job`
- **시간**: 09:00 ~ 15:45 (15분 간격 실행 권장)
- **기능**: **Vestiq 실시간 추천 (Intraday Picks)**
- **로직**:
  1.  Tiingo IEX Websocket/API를 통해 실시간 현재가(Snapshot)를 조회합니다.
  2.  DB에 저장된 과거 2년치 `PriceHistory` 데이터를 로드합니다.
  3.  메모리 상에서 [과거 데이터 + 현재가]를 합쳐 기술적 지표(MACD, MA20 등)를 계산합니다.
  4.  **Vestiq 알고리즘**(Golden Cross + Price > MA20)에 부합하는 종목을 찾습니다.
  5.  찾은 종목은 `VestiqPick` 테이블에 저장되어 화면에 노출됩니다.
  6.  ⚠️ **주의**: 15분마다 조회하는 이 데이터는 **`PriceHistory`에 영구 저장하지 않습니다.** (DB 용량 및 중복 방지)

## 2. 장 마감 후 (Post-Market)

- **파일**: `cron/intraday.py` -> `run_post_market_job`
- **시간**: 16:00 ~ 21:00 (오후 4시 ~ 9시)
- **기능**: **종가 업데이트 & 정밀 스캔**
- **로직**:
  1.  **종가 저장**: Tiingo Daily API를 통해 당일 종가(Close)를 가져와 `PriceHistory` 테이블에 저장합니다.
  2.  **데이터 정리**: `PriceHistory` 테이블에서 **2년(730일)이 지난 오래된 데이터는 자동으로 삭제**합니다.
  3.  **정밀 스캔**: 확정된 종가 데이터를 바탕으로 전체 스캐너(거래량 급등, 변동성 돌파, 추세 반전 등)를 실행합니다.
  4.  결과는 `ScanBatch` 및 `ScanRow`에 저장되어 "Scanner Dashboard"에서 확인 가능합니다.

## 3. 유니버스 관리 (Universe Update)

- **파일**: `cron/universe.py`
- **시간**: 16:00 이후 (하루 1회)
- **기능**: **펀더멘탈 데이터 업데이트 (Update Fundamentals)**
- **로직**:
  1.  기존에 등록된 모든 티커에 대해 Tiingo API로 **재무 데이터**(시가총액, PER, EPS, ROE 등)를 업데이트합니다.
  2.  `FundamentalData` 테이블에 최신 날짜로 기록됩니다.
  3.  참고: **티커 목록(Symbols) 자체의 업데이트**는 Cron에서 제외되었으며, Admin 페이지에서 **수동(Manual)**으로 실행해야 합니다.

## 4. 일일 스냅샷 (Daily Snapshot)

- **파일**: `cron/daily.py`
- **시간**: 16:15 이후 (하루 1회)
- **기능**: **데이터 백업 및 통계**
- **로직**:
  1.  전체 시장 데이터나 포트폴리오 상태에 대한 일일 스냅샷을 생성하여 `data/` 디렉토리 또는 별도 테이블에 기록합니다.
  2.  시스템의 장기적인 추적을 위한 보조 작업입니다.

---

### 요약 스케줄표

| 구분         | 시간 (EST)    | 주요 작업                     | 저장 여부                  |
| :----------- | :------------ | :---------------------------- | :------------------------- |
| **장중**     | 09:00 ~ 15:45 | Vestiq 추천 종목 탐색         | ❌ (Pick만 저장)           |
| **장마감**   | 16:00 ~ 21:00 | 당일 종가 저장 + 정밀 스캔    | ⭕️ (PriceHistory 저장)    |
| **펀더멘탈** | 16:00 ~       | 재무 정보(PER, 시총) 업데이트 | ⭕️ (FundamentalData 저장) |
